# Тестовое задание QA Senior: Автоматизация виджета 3SNET (Python)

Репозиторий содержит E2E тесты для конструктора календаря мероприятий: [https://dev.3snet.info/eventswidget/](https://dev.3snet.info/eventswidget/).

Реализация выполнена на **Python + Playwright** с использованием паттерна **Page Object Model**.

## 🛠 Установка и запуск

Необходим Python 3.8+

1. **Клонирование и установка зависимостей:**
   ```bash
   git clone https://github.com/SanMog/3snet-test-task
   cd 3snet-test-task

   # Создание и активация виртуального окружения (рекомендуется)
   python -m venv venv
   # Для Linux/macOS
   source venv/bin/activate
   # Для Windows
   # venv\Scripts\activate

   # Установка пакетов
   pip install -r requirements.txt
   playwright install
   ```

2. **Запуск тестов:**
   
   После активации виртуального окружения и установки зависимостей тесты запускаются через `pytest`.

   ```bash
   # Запуск всех тестов в headless режиме (без GUI)
   pytest

   # Запуск с отображением браузера (для визуального контроля и отладки)
   pytest --headed --slowmo 500
   ```

## 🏗 Архитектура

* `pages/widget_page.py`: **Page Object**. Инкапсулирует локаторы и логику взаимодействия с UI (включая работу с кастомным jQuery-дропдауном).
* `tests/test_widget_generator.py`: Тестовые сценарии.
* `conftest.py`: Фикстуры pytest для инициализации Page Object.

## ✅ Покрытие тестами

1. **Генерация виджета (Happy Path):**
   * Выбор категории (Affiliate/Blockchain и т.д.).
   * Настройка размеров и валидация атрибутов iframe.
   * Выбор темы и проверка query-параметров.
   * Проверка генерации HTML-кода.
2. **Работа с буфером обмена:**
   * Проверка копирования кода по кнопке.
   * Проверка изменения состояния кнопки ("Скопировано").
3. **Граничные значения:**
   * Валидация полей ввода (авто-коррекция при вводе значений ниже минимальных).

## 📝 Наблюдения (Notes)

* **Custom Dropdown:** Используется нестандартный селект (`.checkselect`), требующий сложной логики кликов (открыть -> выбрать -> закрыть). Реализовано в `select_event_type`.
* **Input Validation:** Поля ввода размеров имеют JS-валидацию по событию `change/blur`. В тестах используется `press("Tab")` для гарантированного срабатывания валидации.
* **Theme Selection:** Для выбора темы (радио-кнопки) пришлось кликать по видимому элементу-обертке `<label>` вместо скрытого `input` для обхода ограничений Playwright на невидимые элементы.
* **Hardcoded IDs:** ID категорий (например, Blockchain=10963) зашиты в HTML. В идеале их следует получать динамически или вынести в конфиг.
